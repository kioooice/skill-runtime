# v0.2 Provider Quality Plan

Date: 2026-05-05

## Goal

Build a small, honest provider-quality evaluation loop for Skill Runtime that measures whether provider-backed governed learning is producing candidates that can survive generation, audit, optional repair, and basic execution smoke checks.

The immediate deliverable is not a new runtime feature. It is a repeatable local evaluation entrypoint that makes provider quality visible.

## Why This Is The Next Core Function

`v0.1.0-alpha` proved that the project has a credible public shape, a maintainer-facing demo, and conservative workflow boundaries.

The next core question is whether provider-backed governed learning is actually good enough to trust as a serious path for candidate generation and review.

That is the right next function because:

- fallback generation is the path taken when deterministic rules do not match
- semantic review is the last quality gate before promotion is even considered
- provider quality currently affects whether governed learning produces useful candidates or template noise
- the project needs evidence about provider quality before it needs broader automation

## Current Provider Limitations

The current provider path has real limits:

- fallback distillation is provider-backed, but mock-backed by default
- semantic audit is provider-backed, but mock-backed by default
- existing tests mostly validate contracts, quality gates, and repair behavior in isolation
- the live DeepSeek smoke covers one end-to-end path, but it requires a real API key
- there is no single local evaluation script that reports fixture-by-fixture loop outcomes honestly

That means the repo can prove contract correctness more easily than it can prove provider-quality outcomes.

## Evaluation Fixtures

`v0.2-provider-quality` should start with a small fixed fixture set that can run locally without a real API key.

Initial fixture types:

1. A demo-provider success case using the local copy-metadata fallback provider plus the local pass semantic review provider.
2. A mock-provider audit-pressure case using the default mock fallback provider plus the default mock semantic provider.
3. A fake DeepSeek repair case using scripted or fake responses that force one bad candidate and one repaired candidate.
4. A fake DeepSeek failure case where generation or semantic review fails and the report must surface the reason directly.
5. A maintainer workflow fixture, preferably review cleanup, which may start as an expected failure if provider-backed generation cannot yet produce an auditable executable candidate honestly.
6. A formal `RuntimeService.distill` fixture that uses local demo providers and may remain an expected failure while the service lifecycle and manual loop still disagree on inferred execution artifacts.

These fixtures should be enough to measure:

- candidate generation shape
- audit outcome
- repair attempt visibility
- execution smoke viability
- lifecycle path visibility
- follow-up recommendation visibility
- maintainer-workflow gap visibility

## Generate -> Audit -> Repair -> Execute Loop

The loop to measure is:

1. Select one evaluation fixture and provider configuration.
2. Generate a staging candidate from an unmatched or forced-fallback trajectory.
3. Audit the generated candidate with static plus provider-backed semantic review.
4. If the fallback provider itself supports one repair attempt, record whether repair was attempted.
5. If the candidate passes the audit, run an execution smoke check with concrete inputs.
6. Report the final outcome without hiding failure.

The evaluation report must show whether the loop stopped at:

- generation failure
- audit failure
- execution smoke failure
- successful candidate execution

The fixture-level `loop_stage` field should use one of these values:

- `generation_failed`
- `audit_failed`
- `execution_failed`
- `execution_passed`

`loop_stage` is the terminal stage where the fixture ended. It does not replace `failure_reason`; it helps the reader scan the report quickly before reading the full reason text.

Repair information stays separate from the terminal stage:

- `repair_attempted`: boolean signal showing whether the fallback provider attempted one repair pass
- `repair_status`: compact repair summary such as `attempted` or `not_attempted`

That keeps repair visibility explicit without confusing it with the final outcome of the loop.

The report should also include `lifecycle_mode` for each fixture.

- `manual_provider_loop`: the evaluation script manually calls `capture_trajectory`, provider-backed fallback generation, audit, and execution smoke in separate steps. This is useful for isolating provider behavior, but it bypasses the normal `RuntimeService.distill` lifecycle entrypoint.
- `runtime_service_distill`: the evaluation script goes through `RuntimeService.capture_trajectory -> RuntimeService.distill -> RuntimeService.audit -> execution smoke`. This is the closer approximation of the formal governed-learning lifecycle.

The next step in `v0.2-provider-quality` is to gradually reduce manual bypasses and move more fixtures onto the formal runtime lifecycle where that can be done without changing architecture just for the demo.

The next concrete artifact after the evaluation script itself is a readable provider-quality baseline document that records which fixtures are expected passes, which are expected failures, and what should count as regression.

That readable baseline now has a machine-readable pair: `docs/provider-quality-baseline.json`. The next quality gate for this mainline is baseline comparison, not a new surface. `scripts/evaluate_provider_quality.py --baseline docs/provider-quality-baseline.json` should keep printing the full report while adding `baseline_comparison`; `--fail-on-regression` should be used only when the caller wants regressions, unexpected failures, or missing fixtures to fail the command.

This remains a provider-quality gate candidate. It is not a dashboard, not a dashboard prerequisite, and not evidence for widening `default-in`.

The next fidelity layer after that baseline is a maintainer-workflow positive control: a deliberately constrained review-cleanup provider fixture that can read review comments and write a cleanup plan without claiming that open-ended maintainer review synthesis is broadly solved.

To make formal-vs-manual differences inspectable, each fixture report should also include a small diagnostic block:

- `staging_file`
- `generated_candidate_provider`
- `inferred_or_used_input_schema`
- `expected_artifacts`
- `produced_artifacts`
- `missing_artifacts`

These fields are there to explain why a fixture stopped where it did. The current goal is to locate lifecycle differences honestly, not to make the report fully green.

One concrete fidelity issue found during this work: the `single_file_copy` deterministic rule was too permissive and could swallow copy-plus-sidecar workflows before fallback distillation had a chance to generate a candidate that preserved the metadata step. That is a workflow fidelity problem in distillation rule matching, not a `default-in` policy problem.

The next fidelity layer after that rule fix is schema fidelity. In the formal `RuntimeService.distill` path, the declared input schema can still diverge from both the execution args used by the evaluation harness and the kwargs keys the fallback-generated candidate actually reads. That is still a governed-learning fidelity problem in distillation and fallback handoff, not a `default-in` policy problem.

The current minimal repair for that gap is intentionally narrow:

- only the fallback-provider path is adjusted
- deterministic-rule metadata stays unchanged
- the runtime extracts simple `kwargs.get("...")` / `kwargs.get('...')` keys from the generated fallback candidate
- when those keys do not match the pre-fallback declared schema, the staged metadata `input_schema` is rewritten to match the candidate's actual kwargs
- the metadata keeps lightweight provenance with `schema_source: "fallback_candidate_kwargs"`

This is metadata/schema alignment for the fallback path, not a policy change and not a relaxation of audit or execution-smoke standards.

## Report Output

The evaluation script should:

- always print the full JSON report to stdout
- optionally write the same JSON report to a user-provided `--output` path
- write that file using UTF-8
- avoid writing to any fixed repository path unless the caller explicitly provides one

That keeps the evaluation local-first and reproducible without dirtying the repository by default.

## Success Criteria

`v0.2-provider-quality` is successful if:

- there is a single local evaluation script for provider quality
- it runs with no real API key by using mock or demo providers
- each fixture produces a structured result line or record
- the result includes lifecycle mode, generation status, audit status, repair attempted or not, execution smoke status, terminal `loop_stage`, the formal-vs-manual diagnostic fields, failure reason, and `recommended_next_action` when available
- failures remain visible instead of being normalized into fake success
- maintainer fixtures are allowed to remain expected failures when that is the honest result
- the repository has a readable baseline document that turns one evaluation run into a stable expectation set for later comparison
- the repository has a machine-readable baseline that lets the evaluation script compare current fixture behavior against the expected fixture set

## Failure Criteria

This work fails if:

- the evaluation path only works with a real provider API key
- the report hides generation or audit failures
- repair attempts are invisible
- execution smoke is skipped without being reported as skipped
- the script implies that a candidate is promotion-ready when the audit failed
- the evaluation work changes runtime entry policy or promotion behavior

## What Will Not Be Changed

This `v0.2` slice will not change:

- dashboard behavior or presentation
- `default-in` scope or runtime entry policy
- automatic promotion behavior
- automatic application of evolution candidates
- maintainer-facing public release positioning

Baseline comparison does not change those boundaries. It is a local quality check for fixture drift and provider-loop regressions, not a product interface and not a default-lane expansion signal.

This is a provider-quality measurement pass, not a product-boundary expansion.

The baseline document for this work is also not a public-traction claim. It is only a local engineering reference for expected fixture behavior.

A maintainer-workflow positive control is also not public-traction evidence. It is only a local quality control that helps distinguish regression from expected failure.

## Why Default-In Remains Unchanged

Provider quality is about governed learning quality after normal task completion, not about making more tasks auto-enter the runtime lane.

Even perfect provider results would not by themselves justify widening `default-in`, because:

- runtime entry policy is about workflow boundary and task risk
- review cleanup and other open-ended maintainer judgment tasks still need conservative handling
- the current evidence set still does not show that broader automatic entry improves outcomes

There is still no evidence supporting expansion of `default-in`, and this `v0.2-provider-quality` work is not intended to create that evidence indirectly.
